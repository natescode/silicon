# Archived: legacy Silicon/Sigil repository

This directory preserves the contents of the **previous** `main` branch of
`github.com/natescode/silicon` as it stood before the monorepo became the
canonical history.

That branch was an independent history (the original 2023 "sigil-compiler"
design/devlog repo) with **no common ancestor** with the current monorepo, so
its files are archived here for safe keeping rather than merged.

- Source ref: `github/main` at commit `3a15f1f8ee0310c51ceff8bfebb228ed2e3a8e32`
- Last commit date: 2026-01-03 16:26:09 -0600
- The full original history is also preserved on the remote branch
  `archive/legacy-main-2023`.
