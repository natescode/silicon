{
  "$comment": "ADR 0017 — bindgen lockfile. Pins the provenance + content hash of the generated FFI surface so CI (bindgen.yml / bindgen.test.ts) fails on any drift between the spec and the three emitted sites. Performance.now() is now generated from real Web IDL via the webidl2 adapter; Math.*/Date.now() are ECMAScript built-ins from the hand-authored table.",
  "generator": "compiler/bindgen",
  "specVersion": 1,
  "toolchain": {
    "runtime": "bun",
    "webidl2": "^24.5.0",
    "note": "Performance.now() is parsed from real Web IDL via webidl2 (devDependency). Math.* and Date.now() are ECMAScript built-ins (no @webref/idl definition) and come from the hand-authored ECMAScript table. The @webref/idl bulk-IDL bundle (+ per-spec version pinning) is the next adapter source."
  },
  "surfaces": [
    {
      "module": "web",
      "group": "math+clock",
      "spec": "compiler/bindgen/src/spec.ts",
      "tier": 0,
      "bindings": 17,
      "sha256": "d2625ff364eef00467519344b5002e736be4a2dd06b0a70a99e7fac1570ab5f4",
      "sites": [
        "compiler/src/strata/modules/web.si",
        "cli/src/host/js-host.ts",
        "playground/playground/web-env.js"
      ]
    }
  ]
}
