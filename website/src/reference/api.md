---
title: "CaaS API surface"
---
## API Report File for "sigil"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function buildRegistry(tree: SyntaxTree, extraSources?: string[]): ElaboratorRegistry;

// @public
export interface CaaSSymbol {
    // (undocumented)
    readonly definitionNode: object;
    readonly definitionSpan?: SourceSpan;
    // (undocumented)
    readonly kind: SymbolKind;
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly type: SiliconType | undefined;
}

// @public (undocumented)
export type ChangeListener = (event: DocumentChangeEvent) => void;

// @public (undocumented)
export interface CheckOptions {
    // Warning: (ae-forgotten-export) The symbol "ModuleRegistry" needs to be exported by the entry point api.d.ts
    moduleRegistry?: ModuleRegistry;
}

// @public (undocumented)
export interface CheckResult {
    // (undocumented)
    readonly diagnostics: readonly Diagnostic[];
    // Warning: (ae-forgotten-export) The symbol "FunctionSig" needs to be exported by the entry point api.d.ts
    //
    // @internal
    readonly _functions: Map<string, FunctionSig>;
    // (undocumented)
    readonly model: SemanticModel;
    // (undocumented)
    readonly tree: SyntaxTree;
}

// Warning: (ae-forgotten-export) The symbol "LowerOptions2" needs to be exported by the entry point api.d.ts
//
// @public
export function compile(source: string, options?: ParseOptions & ElabOptions & LowerOptions2): CompileResult;

// @public (undocumented)
export interface CompileResult {
    // (undocumented)
    readonly diagnostics: readonly Diagnostic[];
    // (undocumented)
    readonly model: SemanticModel | undefined;
    // (undocumented)
    readonly wat: string;
}

// @public (undocumented)
export interface Diagnostic {
    code: string;
    hint?: string;
    message: string;
    notes?: Diagnostic[];
    phase: Phase;
    snippet?: string;
    span: SourceSpan;
}

// @public
interface Document_2 {
    readonly diagnostics: readonly Diagnostic[];
    readonly elabTree: SyntaxTree;
    readonly model: SemanticModel;
    readonly source: string;
    readonly tree: SyntaxTree;
    readonly uri: string;
    readonly version: number;
}
export { Document_2 as Document }

// @public (undocumented)
export interface DocumentChangeEvent {
    readonly document: Document_2 | undefined;
    // (undocumented)
    readonly kind: 'opened' | 'changed' | 'closed';
    // (undocumented)
    readonly uri: string;
}

// @public (undocumented)
export interface ElabOptions {
    extraSources?: string[];
}

// @public
export function elaborate(tree: SyntaxTree, registry: ElaboratorRegistry, _options?: ElabOptions): ElabResult;

// @public
export interface ElaboratorRegistry {
    annotations: Record<string, StrataNode>;
    compiledHandlers: Map<string, {
        invoke: (arg: number) => number;
    }>;
    // Warning: (ae-forgotten-export) The symbol "IRDefExpander" needs to be exported by the entry point api.d.ts
    defExpanders: Map<string, IRDefExpander>;
    // Warning: (ae-forgotten-export) The symbol "DefKindRegistry" needs to be exported by the entry point api.d.ts
    //
    // (undocumented)
    defKinds: DefKindRegistry;
    diagnostics: Diagnostic[];
    // Warning: (ae-forgotten-export) The symbol "IRExpanderFn" needs to be exported by the entry point api.d.ts
    expanders: Map<string, IRExpanderFn>;
    // (undocumented)
    handlers: {
        decl: Map<string, PhaseHandler[]>;
        callSite: Map<string, PhaseHandler[]>;
        annotation: Map<string, PhaseHandler[]>;
        lower: Map<string, PhaseHandler[]>;
        moduleFinalize: PhaseHandler[];
        comptime: Map<string, ComptimeHandler>;
    };
    // (undocumented)
    keywords: Record<string, StrataNode>;
    namedHandlers: Map<string, {
        body: any;
        paramName: string;
    }>;
    // Warning: (ae-forgotten-export) The symbol "StrataNode" needs to be exported by the entry point api.d.ts
    //
    // (undocumented)
    operators: Record<string, StrataNode>;
    pendingDefinitions: any[];
    // Warning: (ae-forgotten-export) The symbol "StratumMeta" needs to be exported by the entry point api.d.ts
    strata: Map<string, StratumMeta>;
    strataHandlerFnNames: Set<string>;
    // (undocumented)
    stratumState: Map<string, Map<string, any>>;
    // Warning: (ae-forgotten-export) The symbol "StructLayout" needs to be exported by the entry point api.d.ts
    //
    // (undocumented)
    structTypes: Map<string, StructLayout>;
}

// @public (undocumented)
export interface ElabResult {
    // (undocumented)
    readonly diagnostics: readonly Diagnostic[];
    // (undocumented)
    readonly registry: ElaboratorRegistry;
    // (undocumented)
    readonly tree: SyntaxTree;
}

// @public
export function lower(tree: SyntaxTree, registry: ElaboratorRegistry, model: SemanticModel, options?: LowerOptions2): LowerResult;

// @public (undocumented)
export interface LowerResult {
    // (undocumented)
    readonly diagnostics: readonly Diagnostic[];
    // (undocumented)
    readonly wat: string;
}

// @public
export function parse(source: string, options?: ParseOptions): ParseResult;

// @public (undocumented)
export interface ParseOptions {
    file?: string;
}

// @public (undocumented)
export interface ParseResult {
    // (undocumented)
    readonly diagnostics: readonly Diagnostic[];
    // (undocumented)
    readonly tree: SyntaxTree;
}

// @public (undocumented)
export type Phase = 'parse' | 'elaborate' | 'typecheck' | 'lower' | 'emit';

// @public (undocumented)
export class SemanticModel {
    // Warning: (ae-forgotten-export) The symbol "SemanticModelOpts" needs to be exported by the entry point api.d.ts
    constructor(opts: SemanticModelOpts);
    // (undocumented)
    readonly allDiagnostics: readonly Diagnostic[];
    get allSymbols(): IterableIterator<CaaSSymbol>;
    diagnosticsIn(range?: SourceRange): readonly Diagnostic[];
    referenceSpans(symbol: CaaSSymbol): readonly SourceSpan[];
    referencesTo(symbol: CaaSSymbol): readonly object[];
    symbolAt(node: object): CaaSSymbol | undefined;
    symbolAtPosition(line: number, col: number): CaaSSymbol | undefined;
    symbolNamed(name: string): CaaSSymbol | undefined;
    typeOf(node: object): SiliconType | undefined;
}

// @public
export type SiliconType = {
    kind: 'Int';
} | {
    kind: 'Int64';
} | {
    kind: 'Float';
} | {
    kind: 'String';
} | {
    kind: 'Bool';
} | {
    kind: 'UInt8';
} | {
    kind: 'UInt16';
} | {
    kind: 'UInt32';
} | {
    kind: 'UInt64';
} | {
    kind: 'Array';
    element: SiliconType;
} | {
    kind: 'Function';
    params: SiliconType[];
    result: SiliconType;
} | {
    kind: 'Distinct';
    name: string;
    underlying: SiliconType;
} | {
    kind: 'Sum';
    name: string;
    variants: string[];
    typeArgs?: SiliconType[];
} | {
    kind: 'Unknown';
} | {
    kind: 'Void';
} | {
    kind: 'Variable';
    name: string;
};

// @public
export interface SourceRange {
    // (undocumented)
    readonly endCol: number;
    // (undocumented)
    readonly endLine: number;
    // (undocumented)
    readonly startCol: number;
    // (undocumented)
    readonly startLine: number;
}

// @public (undocumented)
export interface SourceSpan {
    // (undocumented)
    col: number;
    file: string;
    length: number;
    // (undocumented)
    line: number;
}

// @public (undocumented)
export type SymbolKind = 'function' | 'variable' | 'type' | 'parameter' | 'stratum';

// @public
export class SyntaxTree {
    constructor(program: Program, source: string, file?: string);
    readonly file: string;
    // Warning: (ae-forgotten-export) The symbol "Program" needs to be exported by the entry point api.d.ts
    readonly program: Program;
    readonly source: string;
    withText(newSource: string, options?: ParseOptions): ParseResult;
}

// @public
export function typecheck(tree: SyntaxTree, registry: ElaboratorRegistry, options?: CheckOptions): CheckResult;

// @public (undocumented)
export class Workspace {
    constructor(options?: WorkspaceOptions);
    closeDocument(uri: string): void;
    get documents(): ReadonlyMap<string, Document_2>;
    editDocument(uri: string, newSource: string): Document_2;
    findDefinition(uri: string, line: number, col: number): CaaSSymbol | undefined;
    findReferences(uri: string, line: number, col: number): readonly SourceSpan[];
    getDocument(uri: string): Document_2 | undefined;
    onDidChange(listener: ChangeListener): () => void;
    openDocument(uri: string, source: string): Document_2;
    get registry(): ElaboratorRegistry | undefined;
}

// @public (undocumented)
export interface WorkspaceOptions {
    registry?: ElaboratorRegistry;
}

// Warnings were encountered during analysis:
//
// dist/types/elaborator/registry.d.ts:84:9 - (ae-forgotten-export) The symbol "PhaseHandler" needs to be exported by the entry point api.d.ts
// dist/types/elaborator/registry.d.ts:96:9 - (ae-forgotten-export) The symbol "ComptimeHandler" needs to be exported by the entry point api.d.ts

// (No @packageDocumentation comment for this package)

```
