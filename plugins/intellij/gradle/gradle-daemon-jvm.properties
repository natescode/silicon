#This file is generated by updateDaemonJvm
#
# Gradle daemon JVM criteria.  Gradle 8.10.2's embedded Kotlin DSL compiler
# cannot parse Java 25+, so the daemon must run on JDK 21.  With the foojay
# resolver applied in settings.gradle.kts, `./gradlew` auto-provisions a
# matching JDK on machines that only have a newer Java installed.
toolchainVersion=21
